import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mentalhealthcareawareness',
  webDir: 'www'
};

export default config;
